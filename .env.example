# LDA API — register for a free key at https://lda.gov/api/register/ (manual signup, do this yourself)
LDA_API_KEY=

# Cloudflare R2 (Phase 1+, only needed for publish.py --upload)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PREFIX=lda

# Phase 3 only (weekly briefing email)
ANTHROPIC_API_KEY=
LDA_ALERT_TO=
